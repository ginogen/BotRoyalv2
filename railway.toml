[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn royal_server_optimized:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
HOST = "0.0.0.0"
ENVIRONMENT = "production"

[[services]]
name = "web"
